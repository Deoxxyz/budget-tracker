import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  radioGroup: {
    display: 'auto',
    justifyContent: 'center',
    marginBottom: '-10px',
  },
  button: {
    marginTop: '40px',
  },
}));
